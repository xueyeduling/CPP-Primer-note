#include <iostream>
#include <vector>
#include <list>
#include <algorithm>
using std::cin;
using std::vector;
using std::list;

int main()
{

#pragma region a
	//vector<int> vec;
	//list<int> lst;
	//int i;
	//while (cin >> i)
	//	lst.push_back(i);
	//vec.resize(lst.size());	// 修改
	//std::copy(lst.cbegin(), lst.cend(), vec.begin());
#pragma endregion

#pragma region b
//vector<int> vec;
//vec.reserve(10); // reverse将在9.4节(第318页介绍)
//vec.resize(10); // 这样才有用
//fill_n(vec.begin(), 10, 0);  
#pragma endregion

	system("pause");
	return 0;
}