#include <string>
#include <vector>
#include <map>
//#include <algorithm>

using namespace std;

int main(void)
{
    map<string, vector<int>> sivmap;
    map<string, vector<int>>::iterator iter = sivmap.find("");
    
    system("pause");
    return 0;
}